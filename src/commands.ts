const BUTTON = {
    name: "button",
    description: "Creates a button",
    options: [
            {
                type: 1,
                name: "option",
                description: "button option"
            }
    ]
}

const TEST = {
    name: "test",
    description: "pings the bot in order to test",
    type: 1,
}

export const COMMAND_LIST = [BUTTON, TEST]